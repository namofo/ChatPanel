#!/usr/bin/expect -f
set timeout 20
spawn php artisan shield:install
expect "Do you wish to continue? (yes/no) \[yes\]"
send "yes\r"
expect "Run \`shield:install --fresh\` instead? (yes/no) \[no\]"
send "yes\r"
expect "Name:"
send "Alberto\r"
expect "Email address:"
send "soy@beto.com\r"
expect "Password:"
send "15df45df12fg1M\r"
expect eof